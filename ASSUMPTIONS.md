# 判断記録

- [P0] 部屋認識は「最も時計回りの分岐を選ぶ」有向エッジ巡回で全面を抽出し、最大|面積|の面を外周として除外。理由: 単純で安定。代替: half-edge構造(過剰)。
- [P0] Level に拡張フィールド `nameHints?: {x,y,name}[]` を追加(仕様§4に無い)。理由: サンプル/recon出力が部屋名を運ぶ手段が仕様に無く、Roomは検出のたび再生成されるため位置ベースの命名が最も安定。
- [P0] Project.diagnosis は core では unknown 型で保持。理由: rules→core の一方向依存を守る(循環回避)。
- [P0] RenovationOp の語彙は core に置く(applyOps/validateOps が core 関数のため)。estimate は消費側。
- [P0] add_water_unit に routeNote(給排水経路メモ)を必須フィールドとして型に含めた(仕様§5-M6の「必須入力に」を型で強制)。
- [P0] fixtureの外周壁は structural='suspected'、内部間仕切りは 'unknown'(§2-4)。
- [P0] pnpm11: ビルドスクリプト許可は pnpm-workspace.yaml の onlyBuiltDependencies。verifyDepsBeforeRun: false(テスト実行毎のinstall検査が煩雑なため)。
- [P1] Next.js は仕様の15ではなく16.3.2(現行安定版)。App Router/挙動は同等。
- [P1] 3D回転ズームは three/addons の OrbitControls ではなく自作(球面座標)。drei/型定義のオーバーヘッド回避。
- [P1] ユーザーが描いた・動かした要素は confidence='measured'(§2-7「人の修正は measured として尊重」に従う)。
- [P1] 部屋名の永続化は nameHints(重心座標+名前)方式。部屋は検出のたび再生成されるため。
- [P1] DesiredUse は9リテラル(宿を住宅宿泊/簡易宿所に分割)。UI上は「宿」1カードでサブ選択にし、仕様の「8種」との見た目を保つ。
- [P2] 診断レポートはHTML文字列をiframe srcDocに表示(印刷はiframe.print())。生テキスト直接レンダリング禁止(§10-5)はesc()で担保、テストあり。
- [P2] ConfirmDeskに「供給事業者」は追加せず、gas-kojiの確認先は建築士に寄せた(仕様§5-M2の型との整合を優先)。
- [P2] 用途×用途地域マトリクスは建築基準法別表第二の考え方に基づく簡略値。すべて「目安」表記で、正確な可否は都市計画課へ誘導。
- [P2] 田園住居のカフェ=conditional(農産物関連に限る趣旨の注記は将来detailへ)、事務所は特殊建築物でないため用途変更200㎡ルールの対象外にした。
- [P5] 壁抽出は3D平面フィットではなく「壁帯(床+400〜天井-400mm)の2D射影に対する逐次直線RANSAC」。合成データで十分に安定し、依存もnumpyのみで済むため。
- [P5] GT照合テストは端点一致ではなく被覆率(80%)で判定。T字分割で線分数が変わるため。
- [M7] Project に拡張フィールド surveyNotes / todoDone を追加(§4に無い)。所見とToDo状態の保存先が仕様に無いため。
- [M7] 認証なし(§3-2)。.data/projects はローカル保存のみで、住所等の外部送信はgeocodeとlive時LLM入力のみ(§10-6)。
- [M8] ConfirmDesk「供給事業者」は追加していない(P2判断を維持)。
- [改善2] ナレッジ層はLLMではなく決定的ルール(状態→問い)。理由: 同じ状態で同じ問いが返る安心感と、断定・数値混入の防止。文面は空き家活用パターンランゲージ(建石ほか)から再構成し、パターン名・実名は含めない。
- [改善2] zustand persistの対象はモデル・実測・ピン・診断・3案・ToDo等のデータのみ。tool/選択/履歴は揮発。
- [改善2] 壁ピック許容: 素点420mm→スナップ点80mm の二段。半モジュール(455mm)未満なので隣の壁を誤分割しない。
- [LP] ランディングHTML→JSX変換はスクリプトで機械的に実施（class→className、SVGのkebab属性→camelCase、style→オブジェクト、void要素の自己終了、コメント→JSXコメント）。手作業の写経より差分が出にくい。
- [LP] landing.css は `.landing` にスコープ。理由: `section{padding-block:104px}` 等がアプリの `<section>`（/app/project）を破壊するため。`:root` トークンだけは globals.css に移して全体共有（DESIGN.md §6-1）。
- [LP] 「動画をアップロード」は現状Web経路が無い（recon は Python + fixture のみ、モデル未取得）。CTAラベルを実態に合わせて変更した。ヒーロー見出し「動画一本で」等のコピーは建石さんの判断領域なので変更していない。
- [最大化] 動画のコマ出しはサーバーffmpegではなくブラウザ(video+canvas)で実施。理由: ①依存が減る ②大きな動画を送らずに済む ③公開時もそのまま動く ④実際にこのMacのffmpegが壊れていた。サーバー側APIは画像の永続化専用（動画フォールバックは残置）。
- [最大化] 下絵の縮尺は「2点＋実寸」で決める。写真のEXIFや動画メタからは実寸が出ないため、人が知っている寸法（柱間・畳の長辺など）を起点にするのが最も確実。
- [最大化] エディタのviewBoxを自動フィットから明示ビューへ変更。自動フィットは描くたびに視点が動き、下絵をなぞる用途では致命的だった。
- [最大化] 単価の上書きは WorkItem を書き換えず `applyPriceBook` で写像。マスタは常に初期値のままで、取り込みは取り消せる。
- [全部やる] 三田地域パックは公式ページ確認済みだが `verified:false` を維持。理由: 制度は年度で変わり、金額・期間・要件は窓口確認が必須のため。「調べる手間を消す」のが目的で「確認を省く」のが目的ではない。
- [全部やる] ランディングの図版を自作イラストに置換。理由: CC BY-SA 4.0 はクレジット表示＋継承が条件で、プロダクトサイトの制約になる。DESIGN.md §5 が「全てSVGで自作しているので外部素材は不要」と書いており、思想とも整合する。自前写真への差し替え手順は CREDITS.md に残した。
- [全部やる] ナレッジ層に `urgent` を追加。安全・権利・契約の警告は「こころ→実務」の順序より優先する。黙っていると事故や停止に直結するため、この一点だけ順序を破る。
- [公開] 保存先をサーバーからブラウザへ全面的に移した。理由: 認証を入れるより、そもそもサーバーに個人情報を置かない方が安全で、公開環境でも壊れない。将来チームで共有する必要が出たら、core の `Repository` インターフェースを D1 等の実装に差し替える（口は残してある）。
- [公開] 下絵はアップロードせずデータURLで保持。端末の保存領域を圧迫しないよう長辺1400px/JPEG0.82に落とし、3MBを超える場合はさらに品質を落とす。
- [公開] 既定はモック。公開サイトで LLM_MODE=live にすると訪問者の操作で課金が発生するため、netlify.toml にその旨を明記した。
