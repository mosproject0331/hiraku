# 次にやること(優先順)

## 残っているのは、判断・実データ・現場だけです

1. [human] **単価の実データ投入** — `/app/prices` でテンプレCSVを取得 → 自社の積算単価を入れて取り込む。
   取り込んだ項目は自動で「実データ」扱いになり、見積の「参考値・要検証」バッジが消えます
2. [human] **ルール25本の最終確認** — `/app/rules` を印刷して赤入れ →
   `packages/rules/src/rules/all.ts` を修正。
   （Claudeが2026-08-22に8箇所を精査・改善済み。実務目線の最終確認だけ残っています）
3. [human] **三田地域パックの数字の裏取り** — `packages/regionpack/src/packs/sanda/`
   公式ページから収録済みですが、金額・期間・要件は年度で変わります。窓口で一度確認してください
4. [human] **免責文の最終文言チェック** — `packages/report/src/html.ts` の `DISCLAIMER`
5. [human] `ANTHROPIC_API_KEY` を設定して `LLM_MODE=live` の実挙動確認
   （未設定でも全機能が動きます。現在の状態は `/app` のバッジで確認できます）
6. [human] 自前写真への差し替え（任意） — 現在は自作イラストで成立しており、
   ライセンス上の制約はありません。写真のほうが強いと判断したときに `docs/CREDITS.md` の手順で

## 公開するとき

7. Netlify で公開 — `netlify.toml` を用意済み。リポジトリを繋いで環境変数を入れるだけ。
   ただしプロジェクトの保存(`/api/projects`)はサーバーのファイルに書くため、
   公開環境では再デプロイで消えます（ブラウザ側には残ります）。
   本格運用するなら Repository を Cloudflare D1 等に差し替えてください
8. 認証（現在は認証なし。個人利用・チーム内利用を想定）

## 将来フェーズ（設計は壊さないこと）

9. 動画からの**自動**間取り生成（現在は「下絵をなぞる」半自動で実用可）。
   モデル選定と商用利用可否の確認が前提 → `docs/model-licenses.md`
10. Modalへのデプロイと実動画での再構成テスト → `workers/recon/modal_app.py`
11. 国土数値情報(A29)による用途地域の自動判定 → `docs/opendata-integration.md`
12. 動画アップロード→ジョブキュー→結果通知
13. ナレッジ層の拡充 — 現在24ヒント（27パターン中16参照）。残りのパターンと文面レビュー
14. レーザー距離計(DISTO/GLM)の Web Bluetooth 実装（実機検証が必要）

## 3D・見積書まわりで残していること

15. [human] **見積書の自社情報を入れる** — `/app/quote` の「自社情報」。
    社名・住所・TEL・担当・インボイス登録番号・社印。一度入れれば次回も使えます
16. [human] **単価を自分の値に直す** — 取り込んだままの行は画面上部に警告が出ます。
    繰り返し使う単価は `/app/prices` の単価データに入れておくと取り込みに反映されます
17. 実機（iPhone / Android）での3Dの体感確認。段階は「自動」で決まりますが、
    重いと感じたら「軽い」に切り替えられます
18. 太陽の向きは方位ではなく「主要な開口の外」から決めています。
    本当の方位を使うなら、物件の向きを入力に足す必要があります
19. パースの生成には Gemini のAPIキー（利用者のブラウザにのみ保存）が要ります。
    公開サイトでは各自のキーで動きます
