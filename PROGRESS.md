# 進捗ログ

## P0 基盤+コア — 完了
- できたこと: pnpmモノレポ scaffold / packages/core(型・detectRooms・面積畳数・snapToGrid・estimateModule・serialize) / サンプル間取りfixture(土間+廊下+和室3室) / vitest 13本 green
- やらないことにした事: corepack(Node25で非同梱)→ npm -g pnpm。package.jsonのpnpmフィールド(pnpm11が読まない)→ pnpm-workspace.yamlに設定。

## P1 エディタ — 完了
- できたこと: apps/web(Next.js16/Tailwind4/zustand) / SVG 2Dエディタ(描画・選択・ドラッグ・開口・削除・寸法・確度3色・undo/redo・部屋名編集) / three.js 3Dプレビュー(回転ズーム自作) / サンプル読込・JSON入出力・グリッド吸着・モジュール推定 / ブラウザ実機確認済(サンプル5部屋認識・壁削除で部屋結合と面積追随・3D同期)
- やらないことにした事: drei(OrbitControls用に重い)→カメラ操作を自作。Next15指定→16.3.2(現行安定版)を採用しASSUMPTIONSに記録。

## P2 診断 — 完了
- できたこと: rules25本+81テスト / zoning簡略マトリクス / 内見チェックリストデータ / report(診断・モードA、印刷CSS、XSSエスケープ、確認先マトリクス=窓口×質問文) / web: /wizard(モードA) /diagnose(モードB 11画面ウィザード+GSIジオコーダfallback付き) / ブラウザ確認済(モードA通し・モードB初画面)
- やらないことにした事: ダークモード対応(globals.cssの自動ダークで文字が消えた)→単一ライトテーマに固定。

## P3 改修+見積 — 完了
- できたこと: core(takeoff/validateOps/applyOps+8テスト) / estimate(工事項目マスタ40・見積エンジン・3区分レンジ・構造警告・7テスト) / llm(mock会話2往復→3案・live経路+zod検証+1リトライ→mockフォールバック・2テスト) / web(/plan: チャット→3案カード→内訳手順→間取りへ適用) / ブラウザ通し確認済(サンプル→相談→3案表示・参考値バッジ)
- やらないことにした事: zustand永続化(リロードで消えるのは既知。Repository実装はP6/NEXT_STEPSへ)。<a>遷移はstoreが消えるためnext/linkに統一。

## P4 実測 — 完了
- できたこと: core(solveConstraints逐次調整+確度更新・計測ナビ+5テスト) / report(現況調査報告書: 平面図SVG・実測一覧・劣化ピン・所見+2テスト) / web(実測入力パネル・計測ナビ・実測一覧削除・劣化ピンツール・/survey) / DistanceMeterAdapter(手入力+BTスケルトン) / ブラウザ確認済(壁長2本→緑→面積再計算→報告書表示)
- やらないことにした事: DeviceOrientation API連動(実機がない環境で検証不能)→傾きは手入力。厳密最小二乗→逐次調整(仕様が「厳密さより安定性」)。

## P5 recon+統合 — 完了
- できたこと: Python venv(numpy/pytest) / make_fixture(合成点群+GT) / planes(床z推定・2D線RANSAC自前実装) / walls(逐次抽出・マンハッタン整列・統合・角スナップ・T字分割) / scale / export(core JSON) / pipeline一気通貫 / frames(ffmpeg) / FeedForwardReconstructorスケルトン / ifc_export(optional・未導入はskip) / modal_app雛形 / pytest 5本green / recon出力JSONをcoreがdeserialize→3部屋認識(エディタのJSON読込で取込可) / docs/model-licenses.md(ライセンス未確認表) / docs/opendata-integration.md
- やらないことにした事: open3d(重依存)→numpy自前RANSAC。GPU実推論(§0-7)→fixture経路のみ。

## M7/M8 統合 — 完了
- できたこと: Repository IF(core)+LocalJsonRepository(.data/projects) / API(/api/projects, /api/projects/[id], /api/qa) / /project画面(保存・読込・タブ: 概要ToDo/診断/現況/改修3案・AI質問=reportQA mock) / 確認事項ToDo(診断のconfirmWith×questionsをチェックリスト化) / regionpack(schema+sandaプレースホルダ+診断レポートに地域情報セクション配線) / ブラウザ確認済(保存→一覧→表示)
- やらないことにした事: 認証(§11)。プロジェクト削除UI(APIのみ実装)。

## P6 磨き — 完了
- 全テスト: TS 125本 + pytest 5本(+ifcopenshell optional skip1) green。README/NEXT_STEPS整備。

## 改善ラウンド2(もっと良く) — 完了
- できたこと:
  - **永続化**: zustand persist(localStorage)。リロードしても間取り・実測・診断・3案・ToDoが残る(従来は全消失)
  - **T字接合**: 壁の中間をクリックすると壁を分割してノードを挿入(core splitWallAt+4テスト)。壁の途中から壁を描いて部屋を割れる(ブラウザ確認: 和室B→2部屋)。同一壁の連続分割ID衝突も修正
  - **場づくりナレッジ層 v0(§15)**: packages/knowledge。空き家活用パターンランゲージ由来の問いを、状態に応じて/projectに表示。パターン名は一切出さない(出典は内部フィールドのみ)。こころ=問い/実務=はっきり案内の二層。6テスト
  - **診断ウィザードUX**: 選択肢クリックで自動的に次の画面へ
  - **グリッド吸着のundo保持**(loadModelで履歴が消えていたバグ修正) / **explainerロール**(§8)のプロンプトとexplain()を追加
- やらないことにした事: ビフォーアフター画像生成(素人が本気にする。信頼を壊す)。ヒントのLLM生成(決定的なルールで十分・数値/断定の混入リスクを避ける)。

## ローカル実行の整備 — 完了
- run.command(ダブルクリック起動): 本番ビルドで `next start`。二重起動ガード、初回install/build自動、pnpm不在時の案内、ブラウザ自動オープン
- dev.command(開発モード)も併設
- 検証: 本番ビルド成功 → 全7ページ200 → /api/qa 応答 → run.command経由で88ms起動 → ブラウザ表示・localStorage永続化を確認
- オフラインで全機能動作(LLM_MODE未設定=mock)。同一LAN内のスマホからもアクセス可

## ランディングページ実装 — 完了
- できたこと:
  - **`/` にランディング移植**: `hiraku-landing_3.html` を JSX 化。全10セクション（浮遊ピルヘッダー／中央ヒーロー＋プロンプトカード／機能6枚／コラージュ／3本柱／布地パネル／現地調査2×2＋BEFORE-AFTER／墨地バンド／網点＋引用／エディトリアル／料金／FAQ／最終CTA／フッター）を再現
  - **画像を外部化**: base64 4枚を `public/img/` へ（687KB→51KB のHTML）。CREDITS.md の差し替え手順を実態に合わせて更新
  - **CSSを `.landing` にスコープ**: `body{}` `section{}` 等がアプリ側に漏れないよう機械変換。`:root` トークンのみ `globals.css` で全体共有
  - **アプリを `/app` 配下へ移設**（DESIGN.md §6-2）。全内部リンクを追従
  - **CTAを実機能へ配線**: ヒーローのチップ選択がプロンプト文と連動し、`/app/diagnose?use=` で用途を引き継ぐ（ブラウザで「シェアハウス」の引き継ぎを確認）。3本柱→診断／間取り／改修相談。ヘッダー・料金・最終CTA→`/app`
  - **確度3色をDESIGN.md値に統一**（`#a8a29a`／`#c08a12`／`#2f7a58`）。図面キャンバスの地を `--sunken`、グリッドを `--border-soft` に（§6-3）
  - **アプリ本体もトークン化**: `.hb-*` 共通クラスを globals.css に定義し、`/app` トップとエディタのツールバー・サイドバーに適用
- やらないことにした事: 「動画をアップロード」CTA→動画からの再構成はWeb側の経路が未実装のため、ラベルを「間取りをつくる」に変更して `/app/editor` へ。過大な訴求を避ける（§10 断定しない原則と整合）

## 「できることを最大限」ラウンド — 完了
- **動画→間取りが本当に使えるようになった（最大の穴を塞いだ）**
  - 下絵(Backdrop)を core に実装。動画のコマ／間取り図の写真を図面座標に置き、その上をなぞって作図できる
  - **コマ出しはブラウザ内で完結**（video要素+canvas）。ffmpeg不要・アップロード不要・公開時も動く。
    ※このMacのffmpegはx265リンク切れで壊れており、サーバー側ffmpegに依存しない判断が正解だった
  - **実寸合わせ**: 長さのわかる2点を指定すると縮尺が決まる（core `calibrateBackdrop`、基準点は動かない）。
    ブラウザ実測で外周9,094mm（指定9,100mm）を確認
  - 下絵の移動・回転・濃さ調整。選んだコマだけを `/api/media` に保存して永続URL化
- **図面エディタの視点を安定化（実バグ修正）**
  - viewBoxが描画のたび自動フィットして視点が飛んでいた／下絵が範囲計算に入っていなかった
  - → 明示的なビュー状態＋ホイールズーム（カーソル位置固定）＋Option+ドラッグでパン＋「全体を表示」
  - グリッドはビュー基準で描き、ズームしても線幅が破綻しない
- **単価を実データに置き換えられるようにした**（人間TODOの摩擦を除去）
  - `/app/prices`: テンプレCSV取得 → 自社の単価を入れて取り込み → 該当項目が `verified` になり
    見積カードの「参考値・要検証」バッジが自動で消える。CSVパーサはカンマ・¥・引用符に対応、エラー行を明示
- **ルール25本のレビューシート** `/app/rules`: 利用者に出る文面・確認先・質問文を一覧＋印刷。チェック管理つき
- **ランディングのコピーを実装に合わせた**: 「自動生成」→「下絵にしてなぞる」。見出しも「動画一本から、空き家の可能性を確かめる」に
- テスト: TS 143本 + pytest 5本 green

## 「できる限り全部そっちで」ラウンド — 完了
- **三田地域パックを実データに**（人間TODO #4 を消化）
  - 三田市・兵庫県の公式ページを確認し、ダミーを全撤去。空き家リフォーム補助（上限100万／地域交流拠点型200万、
    対象は市街化区域内・築20年以上・6か月以上空き家・水回り10年以上未更新、工事費100万円以上）、
    空き家バンク登録促進補助（上限10万）、譲渡所得3,000万円特別控除、都市政策課 079-559-5118、
    空き家バンク専用 079-559-5128、宝塚健康福祉事務所 0797-62-7314、地図情報のURLまで収録
  - 制度は年度で変わるため全件 `verified:false`（「要・窓口確認」バッジ）のまま。レポートにURL・電話・注記つきで出る
  - ローカル知見として「調整区域が広い」「補助対象は市街化区域内という制度の食い違い」「地域交流拠点型なら上限が2倍」を追加
- **CC写真を撤去し、図版をすべて自作に**（人間TODO #1 の法的制約を解消）
  - `scene-trace.svg`（座敷＋スキャンする人物＋点群、DESIGN.md §5-4 の色指定に準拠）
  - `scene-work.svg`（改装中の土間、エディトリアル背景）
  - `scene-halftone.png`（室内の明るさ場を合成 → cell=7px/25°/係数.60/ガンマ.72 で網点化。§5-7②に準拠）
  - CC素材は `docs/_unused-photos/` に退避。クレジット表示義務が消えた
- **ffmpeg を修復**（x265のリンク切れ → ca-certificates のリンクを直して x265・ffmpeg を再導入。9.0.1で復旧）
  Python側 `recon.frames.extract_frames` の動作も確認済み
- **ルール25本を自己精査して8箇所改善**: 類似用途なら確認申請不要の論点／民泊の消防法令適合通知書と家主不在型／
  簡易宿所は建築側（避難・内装制限・非常用照明）も同時に効く／検査済証なしは台帳記載事項証明書を先に当たる順番と
  費用時間の桁／未接道でも43条の許可という道／シェアハウスは人数と住戸の独立性で扱いが変わる／
  地目が畑でも実態が宅地のケース／浄化槽は年間維持費も要る
- **ナレッジ層を11→24ヒントに拡充**（27パターンのうち16パターンを参照）。相続登記・契約年数・補助金のあと払いなど実務も追加
- **安全警告が押し出される実バグを修正**: ヒント増加で施工WSの保険警告が上位から消えていた。
  `urgent` を導入し「取り返しのつかない実務 → こころ → その他の実務」の順に。UIでも区別表示
- **LLM接続状態の表示** `/api/status` と `/app` のバッジ（モック／接続中）
- **公開用の設定** `netlify.toml`（保存先の制約もコメントに明記）
- テスト: TS 152本 + pytest 5本 green
